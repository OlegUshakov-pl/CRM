# CRM

<!-- Django 6.0.6 + Tailwind CSS 4 + Alpine.js + HTMX -->

<!-- Quick Start -->

```bash
# Install Python dependencies
python -m pip install -r requirements.txt

# Install Node dependencies
npm install

# Build Tailwind CSS
npm run build

# Run migrations
python manage.py migrate

# Create superuser
python manage.py createsuperuser

# Start dev server
python manage.py runserver
```

<!-- Apps: core, accounts, companies, contacts, projects, materials, tasks, notes, documents, parts, assistant, generator -->
<!-- AI Assistant: chat with Ollama models, voice input, commands -->
